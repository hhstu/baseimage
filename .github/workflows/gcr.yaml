name: gc


on:
   workflow_dispatch
jobs:

  build:

    runs-on: ubuntu-latest

    steps:
    - uses: actions/checkout@v2
    - name: Build the Docker image
      run: |
        docker buildx create --use
        docker login --username=${{ secrets.DOCKER_USERNAME }} --password=${{ secrets.DOCKER_PASSWORD }} 
        echo 'FROM gcr.io/datadoghq/agent:latest' > Dockerfile
        docker  buildx build -t basefly/datadoghq-cluster-agent   --platform=linux/arm64,linux/amd64  .  --push
        echo 'FROM gcr.io/datadoghq/agent:latest' > Dockerfile
        docker  buildx build -t basefly/datadoghq-cluster-agent   --platform=linux/arm64,linux/amd64  .  --push
